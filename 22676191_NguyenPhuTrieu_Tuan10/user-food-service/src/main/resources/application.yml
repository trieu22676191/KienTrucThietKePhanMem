server:
  port: 8081

spring:
  application:
    name: user-food-service
  datasource:
    url: jdbc:h2:mem:userfooddb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
