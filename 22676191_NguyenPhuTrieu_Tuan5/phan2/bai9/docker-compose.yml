version: '3.8'
services:
  flask-app:
    image: python:3.9-slim
    container_name: flask_b9
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "5000:5000"
    command: bash -c "pip install -r requirements.txt && python app.py"
